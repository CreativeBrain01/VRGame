using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DebugBullet : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        //Debug.Log("Update" + GetComponent<Rigidbody>().velocity);
    }
}
